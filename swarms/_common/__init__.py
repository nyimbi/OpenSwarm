"""Tools and helpers shared across multiple swarms.

Lives separate from `shared_tools/` at the repo root (which is for tools
shared across the original 8 OpenSwarm agents) and from any single swarm's
`shared_tools/`. Anything here is genuinely cross-swarm.
"""
