"""SoftDev — software development swarm.

Eight agents: orchestrator (routing only), architect (plans/designs),
coder (implements), reviewer (catches bugs), tester (writes/runs tests),
doc_writer (updates docs to match code), researcher (library/codebase
lookups), devops (CI/CD, build pipelines).
"""
